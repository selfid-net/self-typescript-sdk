import SelfSDK from '../src/self-sdk'

/**
 * Authentication service test
 */

let sdk: SelfSDK

describe('Authentication service', () => {
  beforeEach(async () => {
    sdk = await SelfSDK.build('<appID>', '<appSecret>', '<storageKey>')
  })

  afterEach(async () => {
    sdk.stop()
  })

  it('request', async () => {
    expect(true).toBeTruthy()
  })

  it('generteQR', async () => {
    expect(true).toBeTruthy()
  })

  it('generateDeepLink', async () => {
    expect(true).toBeTruthy()
  })

  it('subscribe', async () => {
    expect(true).toBeTruthy()
  })
})
