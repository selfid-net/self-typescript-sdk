import SelfSDK from '../src/self-sdk'
import Fact from '../src/fact'

/**
 * Facts service test
 */

let sdk: SelfSDK

describe('Facts service', () => {
  beforeEach(async () => {
    sdk = await SelfSDK.build('<appID>', '<appSecret>', '<storageKey>')
  })

  afterEach(async () => {
    sdk.stop()
  })

  it('request', async () => {
    expect(true).toBeTruthy()
  })

  it('requestViaIntermediary', async () => {
    expect(true).toBeTruthy()
  })

  it('subscribe', async () => {
    expect(true).toBeTruthy()
  })

  it('generateQR', async () => {
    expect(true).toBeTruthy()
  })

  it('generateDeepLink', async () => {
    expect(true).toBeTruthy()
  })
})
