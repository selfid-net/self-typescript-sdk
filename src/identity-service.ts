export default class IdentityService {
    devices(selfid: string): string[]{
        let devices:string[] = []
        return devices
    }

    publicKeys(selfid: string): string[] {
        let pks:string[] = []
        return pks
    }

    get(selfid: string) {
        return true
    }
}